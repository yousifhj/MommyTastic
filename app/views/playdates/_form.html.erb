<%= form_with(model: playdate, local: true) do |form| %>
  <% if playdate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playdate.errors.count, "error") %> prohibited this playdate from being saved:</h2>

      <ul>
        <% playdate.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :activity %>
    <%= form.text_field :activity %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>